logging:
  config: classpath:logging-back-dev.xml
spring:
  datasource:
    druid:
      url: jdbc:mysql://#/blog?serverTimezone=Asia/Shanghai&useSSL=false&&characterEncoding=utf8&&useAffectedRows=true